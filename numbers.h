/* Author: Rinn Joireman
   File: numbers.h
   Header file for factorial and gcd functions.
*/

//takes an array of integers and returns the sum
int sum(int nums[], int count);

//takes an empty array of numbers and the max amount of input and fills the array with user input
int getNumbers(int nums[], int max);

//prints out all the numbers in an array
void printNumbers(int nums[], int count);

//takes an array of numbers and averages the values.
float average(int nums[], int count);
